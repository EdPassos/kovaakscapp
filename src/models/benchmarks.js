
class Benchmark {
    constructor(name, description, url, tags) {
        this.name = name;
        this.description = description;
        this.url = url;
        this.tags = tags;
        this.scenarios = [];
    }
}